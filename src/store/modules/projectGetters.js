export const getters = {
  // Getter para obtener la lista completa de usuarios desde el estado
  allProjects(state) {
    // Devuelve el array de usuarios que se encuentra en el estado
    return state.projects;
  },
};
